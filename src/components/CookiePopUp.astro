<!--

<div class="cookie-pop">
    <p>Our website uses cookies. Please review our policy <a href="privacy-policy.pdf">here</a>, and select your
        preferences.</p>
    <div class="cookie-pop-buttons">
        <button class="cookie-pop-settings" id="open-cookies">Cookie Settings</button>
        <button class="cookie-pop-accept" id="acceptAll" name="cookie-pop">Accept All</button>
    </div>
</div>



<script lang="js">


// import { acceptAllCookies } from "../scripts/AcceptAllCookie";
// import Cookies from 'js-cookie'

function acceptAllCookies(){
    Astro.cookies.set('southsideEssential', true, { expires: 28, path: '', sameSite: 'strict'});
    Astro.cookies.set('southsideThirdParty', true, { expires: 28, path: '', sameSite: 'strict'});
    Astro.cookies.set('southsideMarket', true, { expires: 28, path: '', sameSite: 'lax'});
    window.dataLayer = window.dataLayer || [];
    function gtag() { dataLayer.push(arguments); }
    gtag('js', new Date());
    gtag('config', 'G-THCW64VJYF');
}


const popUp = document.querySelector('.cookie-pop')
const settingsBox = document.querySelector('.cookie-settings');
const acceptAll = document.querySelector('#acceptAll');

document.addEventListener('DOMContentLoaded', () => {
    if(Astro.cookies.has('southsideEssential')){
        Astro.cookies.set('southsideEssential', 'true', { expires: 1, path: '', sameSite: 'strict'});
        popUp?.classList.remove('none');
    }
})



function openSettings(){
    if(!popUp?.classList.contains('none')){popUp?.classList.remove('none')};
    settingsBox?.classList.add('none');
}

acceptAll?.addEventListener('click', () => {
        acceptAllCookies();
        popUp?.classList.add('none')
    })
</script>

<style lang="scss">
    .cookie-pop {
    z-index: 2 !important;
    position: fixed;
    bottom: 0;
    background-color: #116d67;
    max-width: 100vw;
    width: 100%;
    display: flex;
    justify-content: space-between;

    @media(min-width:2000px) {
        justify-content: space-evenly;
    }

    @media(max-width:1400px) {
        display: block;
    }

    p,
    a,
    button {
        font-family: 'Poppins', sans-serif;
        font-size: clamp(25px, 1.2vw, 16px);
    }

    p {
        color: #F2F2F2;
        padding: 20px 20px 10px;
    }

    a {
        color: #ff7bac;
    }

    button {
        padding: 5px 10px;
        margin: 0 5px;
        border-radius: 5px;
        font-weight: 600;
    }

    &-settings {
        border: 3px solid #0792ce;
        background-color: #e4e4e4;
        color: #0792ce;
    }

    &-accept {
        border: none;
        background-color: #0792ce;
        color: #F2F2F2;
    }

    &-buttons {
        min-width: 225px;
        max-height: 60px;
        display: block;
        margin: 10px 20px;
    }

}
@media (max-width: 1000px) {

    .cookie-pop {
        &-buttons {
            margin: 0 20px 10px;
        }
}
}

@media (max-width: 780px) {
    .cookie-pop {
            &-buttons {
                display: flex;
            }

            a,
            p {
                font-size: 18px;
            }

            button {
                font-size: 20px;
            }
        }
}

</style> -->