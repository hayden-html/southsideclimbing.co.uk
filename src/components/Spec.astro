---
const { loc, duration, group, cost, age } = Astro.props;
---

{
  (
    <div class="specs">
      {group && (
        <div class="line">
          <img src="/media/icons/people.svg" alt="group icon" />
          <div class="stat">{group}</div>
        </div>
      )}
      {age && (
        <div class="line">
          <img src="/media/icons/check-lg.svg" alt="Tick" id="tick" />
          <div class="stat">{age}</div>
        </div>
      )}
      {loc && (
        <div class="line">
          <img src="/media/icons/location.svg" alt="location icon" />
          <div class="stat">{loc}</div>
        </div>
      )}
      {duration && (
        <div class="line">
          <img src="/media/icons/time.svg" alt="clock icon" />
          <div class="stat">{duration}</div>
        </div>
      )}
      {cost && (
        <div class="line">
          <img
            src="/media/icons/ticket-fill.svg"
            alt="pound symbol"
            id="cost"
          />
          <div class="stat">{cost}</div>
        </div>
      )}
    </div>
  )
}

<style lang="scss">
  .specs {
    display: flex;
    flex-wrap: wrap;
    // max-width: 100%;
    margin-bottom: 5px;
  }
  .line {
    margin: 0 12px 5px 0;
    width: max-content;
    display: flex;
    img {
      display: block;
      height: 21px;
      margin: auto 5px auto 0;
      padding: 2px 0;
    }
    .stat {
      font-weight: 500;
      color: #2e2e2e;
      display: block;
      min-width: max-content;
      margin: auto 0 1px;
      font-size: 18px;
    }
  }

  #cost,
  #tick {
    height: 17px;
  }

  @media (min-width: 768px) {
    .specs {
      display: block;
    }
  }
</style>
